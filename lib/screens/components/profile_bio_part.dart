import 'package:flutter/material.dart';

class ProfileBioPart extends StatelessWidget {
  const ProfileBioPart({super.key});

  ///
  @override
  Widget build(BuildContext context) {
    return const Text('ProfileBioPart');
  }
}
