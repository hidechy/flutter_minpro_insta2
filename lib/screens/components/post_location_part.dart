import 'package:flutter/material.dart';

class PostLocationPart extends StatelessWidget {
  const PostLocationPart({super.key});

  ///
  @override
  Widget build(BuildContext context) {
    return const Text('PostLocationPart');
  }
}
