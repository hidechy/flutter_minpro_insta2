import 'package:flutter/material.dart';

class ProfileRecordPart extends StatelessWidget {
  const ProfileRecordPart({super.key});

  ///
  @override
  Widget build(BuildContext context) {
    return const Text('ProfileRecordPart');
  }
}
